part of fitzgerald;

class QuotesPage extends StatelessComponent {
  Widget build(BuildContext context) {
    return new Scaffold(
      body: new Center(
        child: new Text('Quotes')
      )
    );
  }
}
