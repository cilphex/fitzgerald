part of fitzgerald;

class NovellasPage extends StatelessComponent {
  Widget build(BuildContext context) {
    return new Scaffold(
      body: new Center(
        child: new Text('Novellas')
      )
    );
  }
}
