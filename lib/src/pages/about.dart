part of fitzgerald;

class AboutPage extends StatelessComponent {
  Widget build(BuildContext context) {
    return new Scaffold(
      body: new Center(
        child: new Text('About')
      )
    );
  }
}
