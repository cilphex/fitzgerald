part of fitzgerald;

class PhotosPage extends StatelessComponent {
  Widget build(BuildContext context) {
    return new Scaffold(
      body: new Center(
        child: new Text('Photos')
      )
    );
  }
}
