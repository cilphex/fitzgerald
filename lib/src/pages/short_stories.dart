part of fitzgerald;

class ShortStoriesPage extends StatelessComponent {
  Widget build(BuildContext context) {
    return new Scaffold(
      body: new Center(
        child: new Text('Short stories')
      )
    );
  }
}
